package nl.xservices.plugins.nativekeyboard.lib;

import android.content.Context;
import android.graphics.Typeface;
import android.view.View;
import android.view.ViewGroup;
import android.widget.TextView;

class FontManager
{
  private static final String ROOT = "fonts/";
  static final String FONTAWESOME = "fonts/fontawesome-webfont.ttf";
  static final String IONICONS = "fonts/ionicons.ttf";
  
  static Typeface getTypeface(Context paramContext, String paramString)
  {
    try
    {
      return Typeface.createFromAsset(paramContext.getAssets(), paramString);
    }
    catch (RuntimeException localRuntimeException) {}
    return null;
  }
  
  private static void markAsIconContainer(View paramView, Typeface paramTypeface)
  {
    if ((paramView instanceof ViewGroup))
    {
      ViewGroup localViewGroup = (ViewGroup)paramView;
      for (int i = 0; i < localViewGroup.getChildCount(); i++)
      {
        View localView = localViewGroup.getChildAt(i);
        markAsIconContainer(localView, paramTypeface);
      }
    }
    else if ((paramView instanceof TextView))
    {
      ((TextView)paramView).setTypeface(paramTypeface);
    }
  }
}